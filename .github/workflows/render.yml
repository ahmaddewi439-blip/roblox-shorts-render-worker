name: Render Roblox Shorts Video

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "Workflow OK"
